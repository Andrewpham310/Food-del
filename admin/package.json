{
  "name": "admin-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix admin",
    "start": "npm run dev --prefix admin",
    "build": "npm run build --prefix admin",
    "preview": "npm run preview --prefix admin"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "react-router-dom": "^7.13.0",
    "react-toastify": "^11.0.5"
  }
}